# 阶段 3 测试成果说明

> 备查文档。对应「交互概念：涟漪、锁定、死区、diff、插单」。
> 需求依据：§11 T7/T8/T10、§6.7 diff、§7 插单四策略；BR-25/26/42/43/50。

| 项 | 内容 |
|---|---|
| 记录日期 | 2026-09-14 |
| 复验命令 | `.venv/bin/pytest -q`；`.venv/bin/pytest --cov=engine --cov-report=term -q` |
| 本轮结果 | **30 passed**；`engine/` 行覆盖率 **90%** |

**阶段结论**：不依赖 UI 即可验收 PIN_FIRST 涟漪、锁定占位、死区抑制、插单四策略与 diff 汇总。

---

## 1. 新增测试（7 条）

| 文件 | 规格 | 条数 |
|---|---|---|
| `tests/test_ripple.py` | T7 BR-25/26 | 2 |
| `tests/test_deadband.py` | T8 BR-42 | 1 |
| `tests/test_diff.py` | §6.7 | 2 |
| `tests/test_insert.py` | T10 BR-40/47 | 2 |

---

## 2. 验收要点

| 检查项 | 结果 |
|---|---|
| PIN_FIRST 加量 A 600 → C/E 开工提前；交期不变 | 通过 |
| 锁定 C @9/22 后重排，C 不动 | 通过 |
| A 301 + baseline 死区 → C 保持 9/22 | 通过 |
| 插单 P2 提前期不足 → INFEASIBLE | 通过 |
| 四策略 A/B/C/D + diff「新增 1、移动 2…」 | 通过 |
| engine 覆盖率 ≥ 90% | **90%** |

---

## 3. 实现模块

- `engine/deadband.py` — BR-42 对比 baseline 恢复任务
- `engine/diff.py` — ADD/MOVE/DELAY/LATE/REMOVE + 汇总行
- `engine/insert.py` — `check_insert_feasible`、`schedule_insert` 四策略
- `engine/schedule.py` — `finished_override`、锁定任务、涟漪计数
- `engine/models.py` — `DiffResult`、`InsertCompareResult`、`CapacityOverride` 等

**说明**：插单试排 deliberately 不传 `baseline` 进 `schedule()`，避免死区误抑制挤占（改单仍可用 baseline+trigger）。

**仍登记**：E6/E7（SHOULD）；阶段 4 API / `plan_version` 落库。

---

## 4. 复验

```bash
.venv/bin/pytest -q
.venv/bin/pytest --cov=engine --cov-report=term-missing -q
```
