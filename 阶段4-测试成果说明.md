# 阶段 4 测试成果说明

> FastAPI + SQLite 薄壳，引擎结果与 HTTP 闭环一致；插单试排/落库与冲突持久化已接通。

| 项 | 内容 |
|---|---|
| 记录日期 | 2026-09-14 |
| 复验 | `.venv/bin/pytest -q` |
| 结果 | **40 passed**（含 `tests/test_api_schedule.py` 9 条） |

## 交付物

- `db/`：SQLAlchemy 表（含 `plan_version.conflicts_json`、`wo_insert_log`）、`import_seed_json`、`load_schedule_input`、计划版本读写、`log_insert`
- `api/main.py`：
  - 排产：`/api/schedule/run|what-if|apply`
  - 插单：`/api/schedule/insert`（四策略试排）、`/api/schedule/insert/apply`（落库 + 插单日志）
  - 计划：`/api/plan`、`/api/plan/diff`
  - 冲突：`/api/conflicts`（从版本快照读取）
  - 订单：`POST /api/orders`、`PATCH/GET /api/orders/{no}`
  - 开发：`POST /api/dev/import-seed`
- `scripts/import_seed.py` → `data/scheduling.db`

## 验收

| 项 | 结果 |
|---|---|
| 三单 run 与引擎 bitwise 一致 | 通过 |
| what-if 不升 version | 通过 |
| apply 递增至 version 2 | 通过 |
| 排产不写 `so_order.due_date` | 通过 |
| 用户 PATCH 交期后 run 出 E2 | 通过 |
| GET `/api/conflicts` 与 run 结果一致 | 通过 |
| apply 后 `/api/plan/diff` 可对比版本 | 通过 |
| 新建订单 → 插单试排四策略 → apply 写 `wo_insert_log` | 通过 |

## 启动（开发）

```bash
.venv/bin/python scripts/import_seed.py
.venv/bin/uvicorn api.app_factory:app --reload
```

推荐测试库方式见 `tests/test_api_schedule.py` fixture。

**未做（阶段 5+）**：React 看板 UI、派工单 Excel（T14）、性能日志字段细化。
