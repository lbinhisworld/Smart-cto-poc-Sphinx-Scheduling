---
description: 封印知识库只读；先顾问后工匠；禁止写回约定日
alwaysApply: true
---

# 封印知识库

- 业务为什么、对象有哪些槽：先读 `kb/`，用 `python -m kb explain`（推理在 `packages/sealed_kb`）或顾问角色。不要跳过图现场编主数据。
- 解析员：只改 `kb/`，新卡状态必须是「假设」。客户更新的最小交付是 L1→叶子解释链，人点头才算完成（`harvest_complete` / `python -m kb harvest-check <叶子> --确认`）；禁止只扔孤立 L3。禁止改 `packages/engine/`。禁止点头后擅自把整链改成已封印。顾问交来的质疑只写成假设链，禁止发明库外战略，禁止写「不受 BR-27」。运营允许多主张、多价值流；新流必须撑住主张，禁止无出处复制空流。构建期对话不完整时：先等人写成方案再提取，出处不是聊天；路径 B 未结案不要叫探针补洞。见 `客户会议反馈/认链与探针-升级方案.md` §7。
- 顾问：先判原理 / 质疑 / 细节，再定锚点，用 `python -m kb ask` 走已封印链。树不够密或需求撑不住已有主张就列出缺口/张力，停止。质疑只反问断在哪一层，不改库、不构拟新规则。库不全又在聊新需求：提示先写方案再交解析员，不要当场编卡。
- 工匠：无绑定 Ticket 则拒改。Ticket 须含事实编号、绑哪个对象、禁止项、将改的 `kb/`。禁止写回 `so_order.due_date`。
- 改 `packages/engine/` 或 `config/*.yaml` 必须同时改投影或台账，并跑 `pytest -q tests/test_kb_*.py`。
- 卡片：中文栏名；说明在最底下。运营用「命题」，行业对象用「定义」。不要把 BR 全文写成 L1 主张。
- 行业对象：主林只收格子与量纲；一次倒排切片进运行结果林。有构成才写「属于 / A 的 B」；多端用「关联」，分类用「是一种」。缩写写「别名」。不要抬货物规格当父。运营绑最小槽。见 `客户会议反馈/行业本体提取-升级方案.md`。
- 覆盖：新 BR、新 `engine/*.py`、新 `models` 类必须进 `kb/coverage.yaml` 并挂卡。
