# 阶段 0–1 测试成果说明

> 备查文档。对应开发计划「阶段 0 契约冻结」与「阶段 1 单层倒排与单位/产能」。
> 需求依据：`斯芬克斯-排程服务POC-开发需求规格.md` §11 T1–T4、§14 T1–T5（不含半成品）。

| 项 | 内容 |
|---|---|
| 记录日期 | 2026-09-14 |
| 时钟口径 | `today = 2026-09-15`（周二，种子固定值） |
| 预留产能 | 测试 `reserved_ratio = 0`（运行/演示默认 0.15，见 `config/schedule.yaml`） |
| 复验命令 | `.venv/bin/pytest -q` |
| 本轮结果 | **17 passed / 0 failed**，约 0.07s（pytest 9.1.1 / Python 3.12.7） |

**阶段结论**：组 × 天 JIT 倒排与「版」计量在种子算例上正确。半成品两层、冲突全量、插单尚未纳入本阶段。

---

## 1. 本轮结果摘要

```
.venv/bin/pytest -v
============================= 17 passed in 0.07s ==============================
```

| 文件 | 规格任务 | 条数 | 结果 |
|---|---|---|---|
| `tests/test_seed.py` | 阶段 0 / §14 T1 | 3 | 全绿 |
| `tests/test_uom.py` | §11 T1 | 4 | 全绿 |
| `tests/test_expand.py` | §11 T2 | 3 | 全绿 |
| `tests/test_capacity.py` | §11 T3 | 5 | 全绿 |
| `tests/test_backward.py` | §11 T4 | 2 | 全绿 |

无 skip / xfail。未实现模块（半成品、冲突全量、diff、插单）以 `NotImplementedError` 显式登记，不写假数据。

---

## 2. 阶段验收对照

### 2.1 阶段 0

| 检查项 | 标准 | 本轮 |
|---|---|---|
| 种子加载 | 4 品项、3 订单、S2 库存 130 | 通过 |
| 导入边界 | `engine/` 无 db / http / `datetime.now()` | 通过 |
| pytest 可执行 | `pytest -q` 能跑 | 通过 |

### 2.2 阶段 1

| 检查项 | 标准 | 本轮 |
|---|---|---|
| SO-001 | 9/22:120 + 9/23:300 版；墙钟 3.2h / 8h；人·时 24 | 通过 |
| SO-003 | 9/24:312 版（齐套 9/20） | 通过 |
| SPH 陷阱 | P1 SINGLE 37.5；S2 CREW 40；误乘口径时 T3 数量级用例失败 | 通过（CREW 不乘人数；误用 CREW 工时 11.2 → 33.6） |

---

## 3. 用例明细

### 3.1 阶段 0 · 契约与种子 — `tests/test_seed.py`

| 用例 | 断言要点 |
|---|---|
| `test_seed_loads_four_items_three_orders_s2_stock` | `seed_data.json`：4 品项、3 订单、`stock.S2 == 130` |
| `test_conftest_builds_schedule_input` | `ScheduleInput.today = 2026-09-15`；测试配置 `reserved_ratio = 0` |
| `test_engine_has_no_forbidden_imports` | AST 扫描 `engine/*.py`：禁止 `db` / `requests` / `httpx` / `datetime.now` / `date.today` |

### 3.2 T1 · 单位换算（BR-01 / BR-03 / BR-05）— `tests/test_uom.py`

| 用例 | 断言要点 |
|---|---|
| `test_br01_br03_uom_chain_box_to_board` | P1：1 盒 → **4 版**（走 `md_uom_convert` 链，不硬编码） |
| `test_br01_br03_uom_chain_pcs_to_board` | P1：300 枚 → **12.5 版** |
| `test_br05_p4_computable_pcs_board_box_only` | P4 `computable = True`（POC 仅枚/版/盒参与计算） |
| `test_br03_no_magic_box_to_board_without_chain` | 去掉 BOX→BOARD 边后必须 `UomConvertError`，证明没有魔法数字 4 |

### 3.3 T2 · 需求展开（BR-02）— `tests/test_expand.py`

公式：`qty_board_plan = ceil(订单量归一到版 × (1 + 损耗率))`。本阶段只展开成品工单。

| 用例 | 订单 | 断言 |
|---|---|---|
| `test_br02_expand_p1_100_box` | SO-001 / P1 100 盒 | **420 版**（100 × 4 × 1.05） |
| `test_br02_expand_p2_200_box` | SO-002 / P2 200 盒 | **1236 版**（200 × 6 × 1.03） |
| `test_br02_expand_p4_60_box` | SO-003 / P4 60 盒 | **312 版**（60 × 5 × 1.04）；工单交期 = 订单交期（BR-27） |

### 3.4 T3 · SPH 口径与产能（BR-10 / BR-11 / BR-13）— `tests/test_capacity.py`

| 用例 | 断言要点 |
|---|---|
| `test_br10_sph_single_p1` | P1 SINGLE：`group_rate = 37.5` 版/时（300 枚/时 ÷ 24 × 3 人） |
| `test_br11_crew_no_multiply` | S2 CREW：`group_rate = 40`（人数已含在 SPH 内，**禁止再乘 crew**） |
| `test_br11_e8_when_crew_mismatch` | S2 投入 5 人 ≠ 标定 4 人 → 冲突 **E8** |
| `test_br11_sph_basis_error_magnitude` | P1 420 版：SINGLE 工时 **11.2h**；误标 CREW 变成 **33.6h**（差 3 倍） |
| `test_br13_day_capacity_p1_reserved_zero` | P1 手工组日容量 **300 版**（8h × 37.5，预留 0） |

### 3.5 T4 · JIT 倒排（BR-20 ~ BR-24）— `tests/test_backward.py`

从交期当天往回填工作日；交期当天可排产；不写回 `so_order.due_date`。

| 用例 | 断言要点 |
|---|---|
| `test_br20_br24_backward_basic_so001` | SO-001：9/23 先占 300 版（8h / **24 人·时**），余量落到 9/22 的 120 版（3.2h）；`plan_start=9/22`，`plan_end=9/23` |
| `test_br21_br22_backward_skip_weekend_so003` | SO-003：齐套 9/20、交期 9/24；312 版 ≤ 日容量 320，落在 **9/24 一天**；订单交期仍为 9/24 |

---

## 4. 逐条运行记录（2026-09-14）

```
tests/test_backward.py::test_br20_br24_backward_basic_so001          PASSED
tests/test_backward.py::test_br21_br22_backward_skip_weekend_so003    PASSED
tests/test_capacity.py::test_br10_sph_single_p1                       PASSED
tests/test_capacity.py::test_br11_crew_no_multiply                    PASSED
tests/test_capacity.py::test_br11_e8_when_crew_mismatch               PASSED
tests/test_capacity.py::test_br11_sph_basis_error_magnitude           PASSED
tests/test_capacity.py::test_br13_day_capacity_p1_reserved_zero       PASSED
tests/test_expand.py::test_br02_expand_p1_100_box                     PASSED
tests/test_expand.py::test_br02_expand_p2_200_box                     PASSED
tests/test_expand.py::test_br02_expand_p4_60_box                      PASSED
tests/test_seed.py::test_seed_loads_four_items_three_orders_s2_stock  PASSED
tests/test_seed.py::test_conftest_builds_schedule_input               PASSED
tests/test_seed.py::test_engine_has_no_forbidden_imports              PASSED
tests/test_uom.py::test_br01_br03_uom_chain_box_to_board              PASSED
tests/test_uom.py::test_br01_br03_uom_chain_pcs_to_board              PASSED
tests/test_uom.py::test_br05_p4_computable_pcs_board_box_only         PASSED
tests/test_uom.py::test_br03_no_magic_box_to_board_without_chain      PASSED
```

---

## 5. 已实现 / 显式未实现

### 5.1 本阶段已交付

- `engine/models.py`：§5 枚举与 Pydantic 模型
- `engine/uom.py`：换算链归一到版
- `engine/capacity.py`：`group_rate` / `day_capacity` / 双小时字段 / E8 预检
- `engine/expand.py`：订单 → 成品工单（仅 `FINISHED`）
- `engine/backward.py` + `engine/schedule.py`：锁定占位 + DUE_DESC + 倒排（无半成品）
- `config/schedule.yaml`、`config/weights.yaml`、`seed/seed_data.json`

### 5.2 登记未实现（`NotImplementedError`）

| 符号 | 计划阶段 | 对应规格 |
|---|---|---|
| `expand.expand_semi` | 阶段 2 | BR-30~33，§11 T5/T6 |
| `conflicts.detect_conflicts` | 阶段 2 | E1–E8，§11 T9 |
| `conflicts.earliest_finish` | 阶段 2 | BR-47 |
| `diff.diff` | 阶段 3 | §6.7 |
| `insert.check_insert_feasible` / `schedule_insert` | 阶段 3 | §11 T7/T8/T10 |

因此本轮 **不覆盖**：SO-002 半成品 282 版、改交期 E2、三单全局同排、涟漪/锁定/死区、插单四策略、覆盖率 ≥ 90%（阶段 3 门槛）。

---

## 6. 复验方式

```bash
# 全量（当前即阶段 0–1）
.venv/bin/pytest -q

# 按规格任务拆开
.venv/bin/pytest tests/test_uom.py tests/test_expand.py tests/test_capacity.py tests/test_backward.py -v
```

后续阶段新增测试文件后，请在对应阶段成果说明中另记一轮 `pytest -q`，不要改写本文件的历史数字。
