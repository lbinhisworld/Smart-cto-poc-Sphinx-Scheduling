# 阶段 4 测试成果说明

> FastAPI + SQLite 薄壳，引擎结果与 HTTP 闭环一致。

| 项 | 内容 |
|---|---|
| 记录日期 | 2026-09-14 |
| 复验 | `.venv/bin/pytest -q` |
| 结果 | **37 passed**（含 `tests/test_api_schedule.py` 6 条） |

## 交付物

- `db/`：SQLAlchemy 表、`import_seed_json`、`load_schedule_input`、计划版本读写
- `api/main.py`：`/api/health`、`/api/schedule/run|what-if|apply`、`/api/plan`、`/api/plan/diff`、`/api/orders/{no}` PATCH/GET
- `scripts/import_seed.py` → `data/scheduling.db`

## 验收

| 项 | 结果 |
|---|---|
| 三单 run 与引擎 bitwise 一致 | 通过 |
| what-if 不升 version | 通过 |
| apply 递增至 version 2 | 通过 |
| 排产不写 `so_order.due_date` | 通过 |
| 用户 PATCH 交期后 run 出 E2 | 通过 |

## 启动（开发）

```bash
.venv/bin/python scripts/import_seed.py
.venv/bin/uvicorn api.app_factory:app --reload
```

推荐测试库方式见 `tests/test_api_schedule.py` fixture。

**未做（阶段 5+）**：`/api/schedule/insert` 完整 HTTP、冲突 GET 持久化、React UI。
