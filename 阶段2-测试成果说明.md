# 阶段 2 测试成果说明

> 备查文档。对应开发计划「阶段 2：两层半成品 + 冲突 + 最快可交期」。
> 需求依据：`斯芬克斯-排程服务POC-开发需求规格.md` §11 T5/T6/T9、§14 T6–T7。

| 项 | 内容 |
|---|---|
| 记录日期 | 2026-09-14 |
| 前置 | 阶段 0–1 已绿（§11 T1–T4） |
| 复验命令 | `.venv/bin/pytest -q` |
| 本轮结果 | **23 passed / 0 failed**（含阶段 1 共 17 + 阶段 2 新增 6） |

**阶段结论**：成品 → 二部半成品自动生成与倒排、改交期 E2 与最快可交期 9/29、三单 DUE_DESC 全局排产在算例上成立。

---

## 1. 本轮新增测试（6 条）

| 文件 | 规格 | 条数 | 结果 |
|---|---|---|---|
| `tests/test_semi.py` | §11 T5 | 2 | 全绿 |
| `tests/test_demo_due_change.py` | §11 T6 | 2 | 全绿 |
| `tests/test_conflicts.py` | §11 T9 | 1 | 全绿 |
| `tests/test_schedule_global.py` | seed `all_orders_global` | 1 | 全绿 |

辅助：`tests/helpers.py`（`tasks_of` / `semi_wo_of` / `has_conflict` / `earliest_finish_from_result`）。

---

## 2. 阶段验收对照

| 检查项 | 标准 | 本轮 |
|---|---|---|
| SO-002 默认 | semi 282 版，due 9/25；P2 七段，首段 9/29:36 | 通过 |
| 改期 9/23 | E2 红 + 最快 9/29；订单交期不变 | 通过 |
| 改期 9/30 | 半成品 plan_start=9/18（冻结区）；无 E2 | 通过 |
| 三单同排 | SO-002→SO-003→SO-001；S2 9/25:282 | 通过 |
| BR-50 | 有红警仍出 PLANNED，冲突带 suggest | 通过 |

---

## 3. 用例明细

### T5 · `tests/test_semi.py`

- **282 版净需求**：`ceil(200×2×1.03)=412 − 库存 130`；semi `due=9/25`（成品 `plan_start 9/29 − 4 自然日`）。
- **P2 七段任务**：余量 36 落在最早工作日 9/29；依赖 `offset_days=4`。
- **库存够**：S2=500 时不生成半成品工单（BR-32）。

### T6 · `tests/test_demo_due_change.py`

- **交期 9/23**：E2（RED）；`earliest_finish=2026-09-29`；`so_order.due_date` 仍为 9/23。
- **交期 9/30**：半成品 `plan_start=9/18`（冻结区）；不产生 E2（装得下）。

### T9 · `tests/test_conflicts.py`

- 改早交期后成品工单仍为 `PLANNED`；冲突列表非空且含 `suggest`（不阻断排产）。

### 全局 · `tests/test_schedule_global.py`

- 三单同时倒排：P1/P4 与阶段 1 一致；P2 + S2 与 SO-002 单测一致。

---

## 4. 实现交付物

| 模块 | 内容 |
|---|---|
| `engine/expand.py` | `expand_semi`、`gross_semi_board`（BR-30~33） |
| `engine/schedule.py` | 先成品后半成品、依赖表（BR-34~36） |
| `engine/conflicts.py` | E1–E5、E8；`FROZEN` 黄警；`earliest_finish_finished`（BR-47） |
| `engine/calendar_util.py` | 正排工作日推算（最快可交期） |

**登记未实现**：E6/E7（`detect_e6_e7`）；阶段 3：`diff`、`insert`、涟漪/死区。

---

## 5. 复验

```bash
.venv/bin/pytest -q
.venv/bin/pytest tests/test_semi.py tests/test_demo_due_change.py tests/test_conflicts.py tests/test_schedule_global.py -v
```
