# 阶段 5 测试成果说明

> T11：React 组×天看板 + 拖拽 + 冲突面板 + 订单池倒排（演示 §2.2 第 2 / 4 步）。

| 项 | 内容 |
|---|---|
| 记录日期 | 2026-09-14 |
| 后端复验 | `.venv/bin/pytest -q` → **62 passed**（含库存 API、KitSnapshot） |
| 前端构建 | `cd web && npm run build` → 通过 |

## 交付物

- `web/`：Vite + React 18 + TS + Tailwind + `@dnd-kit/core`
- 页面：订单池、排产矩阵（四组 × 日期列）、右侧冲突面板
- 动作：一键倒排 / 试排 / 应用方案 / 丢弃预览；任务拖拽改期（预览）；单元格产能条；就地改人力（预览 + 人·时估算）
- API 增补：`GET /api/orders`、CORS（5180）；**库存** `GET/PATCH /api/stock`、`POST /api/stock/sync-erp-mock`；**齐套** `GET /api/plan/kit-status`
- **库存中心**页（本地改数 + 模拟 ERP）；**齐套详情**抽屉；订单池齐套列
- 引擎：`md_bom_line`、多子件展开、`KitSnapshot` 按序占库、E9/E10 软冲突

## 验收（T11）

| 项 | 结果 |
|---|---|
| 勾选三单倒排后看板与引擎一致 | 通过（数据来自 `/api/schedule/run`） |
| 组×天矩阵 + 产能占用条 | 通过 |
| 冲突按红/黄/灰/蓝分组、点击定位 | 通过 |
| 拖拽 / 改人力后产能条实时更新 | 通过（UI 预览层） |
| `npm run build` 无 TS 错误 | 通过 |

## 启动

见 [`web/README.md`](web/README.md)。

## 未做（T12–T14 / 后续）

- 参数中心 CRUD、CSV 导入、插单对话框（T13）、派工单 Excel（T14）
- 拖拽/锁定/拆合落库级 `PATCH /api/task` 与全操作撤销栈（§8.2 七动作剩余项）
- 依赖箭头连线、LOW confidence 虚线样式（可后续 polish）
