# 斯芬克斯 · 排程服务 POC — Cursor 规则
# 完整需求见 ./斯芬克斯-排程服务POC-开发需求规格.md（BR-xx 为业务规则编号）

你在本项目中实现一个「生产排程服务 POC」。核心是**倒排排程引擎**（无限产能 + 人工交互 + 冲突预警），不是 CRUD。

## 工作方式（强制）
1. 每个引擎函数**先写 pytest 断言再写实现**（TDD）。测试不通过 = 未交付。
2. 严格按需求文档 §14 的 T1→T14 顺序推进，每步跑 `pytest -q` 并报告结果后再进下一步。
3. 引擎有疑问时，先查需求文档中的 `BR-xx` 编号；仍不确定就停下来问，不要猜。
4. 未实现的逻辑显式 `raise NotImplementedError` 并登记，**禁止用 mock/假数据掩盖**。

## 架构红线
- `engine/` 是**纯函数区**：禁止 import db / requests / datetime.now()。`today` 只由入参传入。
- 数据模型以 `engine/models.py` 的 Pydantic 模型为唯一事实源，API 与 DB 层都从它派生。
- 配置（fence_days / ripple_limit / deadband / reserved_ratio / 优先级权重）写在 `config/*.yaml`，禁止硬编码。

## 业务红线（违反即重写）
- **BR-01**：所有数量计算必须先归一到「版」。禁止拿「枚」或「盒」直接除 SPH。
- **BR-03**：换算率必须走 `md_uom_convert` 换算链，禁止硬编码。
- **BR-11**：SPH 口径 = `CREW`（多人配合）时，人数已含在 SPH 内，**禁止再乘 crew**。
- **BR-27**：**禁止任何写回 `so_order.due_date` 的代码**。交期是锚，只有人能改。
- **BR-34**：必须先排成品、再排半成品；成品变动必须重建半成品工单（含依赖表）。
- **BR-25**：处理顺序决定涟漪方向，**默认 `DUE_DESC`（due_date 降序）**；插单/改单场景切 `PIN_FIRST`（触发变更的工单先占位）。不要自作主张改成"紧急单优先（升序）"——那会把加量单拆散到远期零散空位。
- **BR-40**：禁止"全量重排"作为默认行为，插单/改单走时间栅栏 + 涟漪抑制。
- **BR-47**：物理不可行时明确判定并给出最快可交期，**禁止硬排做不到的计划**。
- **BR-50**：8 条冲突（E1–E8）全部是软约束，只提示不阻断，禁止 raise 终止排产。

## 编码约定
- 数量：版数用 `int`（`ceil` 向上取整）；换算率/工时用 `Decimal`，工时内部 4 位、展示 2 位。禁止 float 参与相等比较。
- 日期：`datetime.date`，不带时区、不带时间。
- 倒排循环必须有防死循环保护（guard > 365 raise SchedulingLoopError）。
- 两个「小时」必须分开存：`hours_wall`（墙钟，排日程用）与 `hours_man = hours_wall × crew`（人·时，算成本用），禁止混用。

## 禁止引入
- 优化/求解类库（OR-Tools、遗传算法、LP 求解器）—— 一期不需要 APS/优化。
- 正排、双向排、有限产能约束求解 —— 一期只用 JIT 倒排 + 组日容量软限制。

## 术语速查
版=系统内部唯一计量单位；SPH=标准小时产能（四元组：值/口径/人数/单位）；
组=手工组|模具组|浇注组|二部半成品组；工单 wo（FINISHED/SEMI）；任务 wo_task=组×天一行；
冻结区=today→+4天(=半成品提前期)；涟漪=一串单开工日往前移；死区=位移<0.5天或工时变化<10%不重排；
what-if=内存副本试排可丢弃；plan_version=每次重排生成版本号用于 diff。

## 封印知识库（第一期）
业务疑问先对照 `kb/`，用 `explain` / 顾问角色回答，禁止跳过知识库猜测主数据或流程。
解析员只许写 `kb/` 且标「假设」；工匠必须持有绑定 Ticket。禁止写回 `so_order.due_date`（BR-27）。禁止改 `packages/engine/` 来「补」知识。
