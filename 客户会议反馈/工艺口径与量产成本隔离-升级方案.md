# 工艺口径与量产成本隔离 · 升级方案（并行 Wave X）

> 2026-09 会后逐字稿 + 客户书面两轮。第一轮：工序树 / 实际成本 / 打样进项目 / 费用分期。第二轮：工作中心与工序分管、实际=工时×实际费率+实际材料、入库结转、未入库成本停工单。  
> **并行专题，不替代、不抢跑 Wave 3（二部拆组）/ Wave 4（设备+关键岗额度）。**  
> 无标准工序清单前 **禁止改 `GroupCode`**。禁止写回 `so_order.due_date`（BR-27）。一期不做 APS。  
> 第二轮是生意约束，先记在本方案；未交解析员、不进 `kb/`。封印主张「按单、不设成品仓」与「入库才算生产完成」并列记张力，不现场调和。

## 1. 为什么单独成波

Wave 3 原假设「二部拆成原料→生巧→半成品三条排程组」。客户后来补了两件事：

1. **一部三个组不是 1→2→3**，多数是一品一组做完再包装；组装没有独立组。
2. **工序 ≠ 工作中心 ≠ 报工**。报工目标颗粒度是「工序 × 工作中心」。再按组名拆引擎，会把两维焊死，成本中心还得拆一次。

因此：已锁死的口径先落文档和一条小实现；拆组仍等生产清单。

## 2. 本波做什么

| 步 | 做什么 | 对外一句话 |
|----|--------|------------|
| **X-A** | 把已确认口径写进总计划 / POC 范围 / 统计方案 | 一部是产线；三级映射是目标；倒排仍组×天 |
| **X-B** | 量产品项成本剔除打样 / 失败轮次工时 | 量产成本干净；打样进费用，不进品项 |
| **X-C** | 看板/BOM 文案对齐（不改算法） | 二部片材可流向一部三组；一部组不是工序链 |

X-A = 本文件与总计划补丁。X-B / X-C 已落地：量产成本接口剔除 `SAMPLE`/`RND` 及报价挂打样单；看板/BOM 写明一部产线与二部片材流向。

## 3. 已锁定口径（可对外说）

### 3.1 一部组 = 工作中心，不是工序链

- 手工 / 模具 / 浇筑是三条产线。多数品只在其中一个组做完，再去包装、入成品仓。
- 少量：多组做配件，在主配件所在组组装。**没有独立组装组。**
- 现有「一品项绑一个 `GroupCode`」继续当默认，覆盖多数路径。
- **本期不加包装组、不做多组配件路由**（确认有包装，未确认占产能）。

### 3.2 二部 → 一部

- 二部半成品片材 / 印刷车间产出 **可以流向一部全部前端**（手工、模具、浇筑）。
- 齐套/半成品库存按品项引用即可，不把二部焊死到某一个一部组。
- 原料 / 生巧 / 半成品 / 印刷 哪些是工序、哪些是工作中心：**未确认，不改引擎。**

### 3.3 三级映射（目标架构，本期不建树）

| 层 | 管什么 | 本期 POC |
|----|--------|----------|
| 工序树 | 产品怎么做（与机器、人无关）；清单归运营管理 | 等生产给标准清单；**不编假树** |
| 工作中心 | 用什么资源（班组/产线/设备组）；客户称「股东投入资源」 | 现有组 = 看板一行、日历、SPH、组×日报工 |
| 报工单元 | 谁在何时做了多少 = **工序 × 工作中心** | 现报工只有工作中心×日；升维等清单 |

一道工序可绑多个中心；一个中心可接多道工序。缺一维不能报工——这是验收后的 MES 口径，不是当前倒排输入。

### 3.4 成本

- **禁止**用标准 backflush 倒挤半成品→成品来掩盖工序浪费。
- **现状必须写明**：产品成本仍是 `hours_man × 标准人时单价`，不是实际成本结转。
- **客户目标（第二轮，本期不实现）**：实际工时 × 实际费率 + 实际材料；结转跟仓库入库，不跟末道报完工；未入库则物料成本停在工单。详见 §6。
- 排程展量继续用标准损耗（BR-02 / BR-31）——那是「要做多少版」，不是结转。
- 打样工时、失败轮次：**归部门/项目费用，不摊进量产品项。** 项目 ROI 整页仍待设计。
- 制造费用一期主线：人工效率 + 设备占位；水电辅助监控。**不做** OEE 公式、折旧计提、电费分摊（等生产回复）。设备占位 ≠ Wave 4 排程额度。

## 4. X-B 实现要点（先测后改）

现状：[db/product_labor_cost.py](../db/product_labor_cost.py) 按 `so_order` / 工单品项汇总；打样在 `crm_sample`，**没有**自动隔离。

建议识别（实现时用测试钉死一种，禁止猜）：

1. 订单 `order_source = SAMPLE`（或等价），或
2. 报价 `crm_quote.sample_code` 已转到该订单，或
3. 工单/任务显式 `cost_bucket = SAMPLE | RND`

量产接口（驾驶舱 Top 品项、`GET /api/hr/labor-cost/by-product`、BOM 计划成本）默认只含量产。打样合计可另字段 `sample_excluded`，不并进品项。

引擎不读此标记。禁止为了隔离去改 `due_date`。

## 5. 明确不做

- 改 `GroupCode`、二部三组进看板（Wave 3）
- 编一份未签字的标准工序树 / 成本中心
- 报工升维到工序 × 中心
- 包装组占产能；多组配件 + 主组组装路由
- 实际成本滚算、标准 backflush
- 项目 ROI 模块、商机 ABC
- OEE、水电入成本、点名到人、按小时/锅排产

## 6. 客户已答（第二轮，生意约束；本期仍不实现）

出处：客户对 Wave X §6 追问的书面回复。拆开「他们要的账」和「POC 现在能演示的格子」。不改 `GroupCode`，不改核算代码，不写回 `kb/`。

### 6.1 工作中心与工序清单分开管理

客户：工作中心代表股东投入资源；工序清单用于运营管理。两份清单分开建。

- 同向 §3.3：路径不能焊进工作中心，也不能再猜「一组 = 一道工序」。
- **没给**我们要的那张表：若干代表品「工序顺序 + 每道可选工作中心」。Wave 3 门槛仍在。

### 6.2 实际 = 实际工时 × 实际费率 + 实际材料

客户否决了试探口径「实际工时 × 标准费率 + 实际投料」。费率和材料都走实际。

- 与 POC 赶架：现状与演示仍是 `hours_man × 标准人时单价`（同一单价看计划/实际人·时差异）。封印链只锁「成本看人·时」，没锁费率种类、没锁材料进品项。
- 本期继续诚实写「不是实际结转」，禁止用 backflush 假装已经按这条公式做了。

### 6.3 结转时点 = 仓库入库

客户：做出来 + 质检过 + 达到入库仓库状态，才是生产完成；否则在制。结转不跟末道报完工。统计方案里「入库对账」被抬成完工/结转口径。

- **张力（不调和）**：已封印主张「按单交付、不设成品仓、不靠现货兑现约定」与「入到仓库才算完成」并列。库里没有入库槽来接这两句。先记张力，等方案认完再交解析员抽假设链。
- 制成/报工现状：组日任务落下、欠产回池；报工确认只进实际人·时。不是入库出在制。

### 6.4 未入库则成本（物料）停在工单

客户确认上一问，并放宽：哪怕转入一部，只要未入库，还是在产品，物料成本停留在工单上。转入一部 ≠ 出在制。

- 排程「半成品完工后成品才能开」是开工依赖，不是成本搬家。
- POC C 把人·时×标准单价汇总到量产成品品项，本来就不是半成品→成品实际结转。

## 7. 仍要客户 / 生产补的（不挡 X-A，挡 Wave 3 与成本二期）

生产：标准工序清单、工作中心清单、8～10 个代表品「工序 → 可选工作中心」（第二轮仍未交这张表）；二部印刷/片材/喷涂/原料/生巧的维；OEE 与电费分摊键。  
总经办：打样费用挂打样单号还是必须先有项目编号。

第二轮之后才问得动、仍不挡本期：

- 实际费率从哪张表来（人事、工单、还是另建）。
- 实际材料按投料单归到哪张 WO；半成品仓是否单独存在。
- 「入库仓库」是履约对账仓、还是客户口头的成品仓——与「不设成品仓」怎么并存，等人点头后再抽卡。
