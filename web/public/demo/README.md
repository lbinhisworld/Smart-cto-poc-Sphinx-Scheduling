# Workbuddy 讲解页（可选静态 HTML）

将 Workbuddy 生成的单页 HTML 放到本目录，例如：

- `schedule-story.html`
- `cost-story.html`

在 POC 内通过 iframe 打开（需先登录）：

- `/demo/story/schedule?embed=/demo/schedule-story.html`
- `/demo/story/cost?embed=/demo/cost-story.html`

未放置 HTML 时，使用内置分步讲解（同路径不带 `embed` 参数）。
